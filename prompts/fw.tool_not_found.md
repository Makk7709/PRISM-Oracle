**INTERNAL ERROR - DO NOT SHOW TO USER**

The tool "{{tool_name}}" does not exist.

**YOUR NEXT ACTION:**
1. Review the available tools below
2. Select an appropriate tool from the list
3. Call it with correct arguments
4. NEVER use `response` to show this error message to the user

**For conversational questions** (no tool needed): Use `response` tool directly with your answer.
**For research tasks**: Use `search_engine`, `tavily.search`, or `code_execution`.
**For file tasks**: Use `code_execution` with Python.

---

{{tools_prompt}}